<!--components/tabBar/tabBar.wxml-->
<view class="tab-bar-component">
    <button wx:for="{{tabBarList}}" class="tab-bar-item" wx:key="{{index}}" style="{{itemFlex}}" bindtap="tapItem" data-item="{{item}}" open-type="{{item.openType}}" bindgetuserinfo="tapItem">{{item.label}}</button>
</view>